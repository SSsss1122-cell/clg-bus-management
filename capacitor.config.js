/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: "com.bus.management",
  appName: "Bus Management",
  webDir: "out",  // static exported folder
  bundledWebRuntime: false
};

module.exports = config;
